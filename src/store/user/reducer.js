// src/store/user/reducer.js
const initialState = {
  id: 3,
};

export default function userSliceReducer(state = initialState, action) {
  switch (action.type) {
    default: {
      return state;
    }
  }
}
